<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jkstack.dsm.user.mapper.UserMapper">

    <resultMap type="com.jkstack.dsm.user.entity.UserEntity" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="tenantId" column="tenant_id" jdbcType="VARCHAR"/>
        <result property="companyId" column="company_id" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="creatorId" column="creator_id" jdbcType="VARCHAR"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
        <result property="modifierId" column="modifier_id" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="loginName" column="login_name" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="userNo" column="user_no" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="wechat" column="wechat" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="position" column="position" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectPageUserByDepartmentId" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT user.* FROM dsm_user user
            LEFT JOIN dsm_department_user d ON user.user_id=d.user_id WHERE d.department_id='${departmentId}'
            ORDER BY d.sort ASC, d.modify_time DESC
    </select>

    <select id="selectPageUserByDepartmentIdAndLike" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT user.* FROM dsm_user user
            LEFT JOIN dsm_department_user d ON user.user_id=d.user_id
            WHERE d.department_id='${departmentId}' AND (user.name LIKE '%${condition}%' OR user.login_name LIKE '%${condition}%' OR user.user_no LIKE '%${condition}%')
            ORDER BY d.sort ASC, d.modify_time DESC
    </select>

    <select id="selectPageUserByNotDepartmentId" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT * FROM dsm_user WHERE user_id NOT IN (SELECT user_id FROM dsm_department_user WHERE department_id='${departmentId}')
    </select>

    <select id="listByDepartmentIds" parameterType="string" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT user.user_id, user.name,user.login_name FROM dsm_user user
            LEFT JOIN dsm_department_user dept ON user.user_id=dept.user_id
            WHERE dept.department_id IN
            <foreach collection="list" item="id" index="index" open="(" separator="," close=")">
                #{id}
            </foreach>
    </select>

    <select id="selectPageUserByNotDepartmentIdAndLike" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT * FROM dsm_user WHERE user_id NOT IN (SELECT user_id FROM dsm_department_user
            WHERE department_id='${departmentId}')
            AND (name LIKE '%${condition}%' OR login_name LIKE '%${condition}%' OR user_no LIKE '%${condition}%')
    </select>

    <select id="listByWorkGroupId" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT user.* FROM dsm_user user
            LEFT JOIN dsm_work_group_user g ON user.user_id=g.user_id
            WHERE g.work_group_id='${workGroupId}'
    </select>

    <select id="selectPageByLike" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT user.* FROM dsm_user user WHERE CONCAT(IFNULL(user.name, ''),
            IFNULL(user.login_name, ''), IFNULL(user.user_no, ''),
            IFNULL(user.phone, ''), IFNULL(user.wechat, ''))
            LIKE '%${condition}%'
    </select>

    <select id="selectLeaderUserListByDepartmentId" parameterType="string" resultType="com.jkstack.dsm.user.entity.UserEntity">
        SELECT user.* FROM dsm_user user
            LEFT JOIN dsm_department_leader leader ON user.user_id=leader.user_id
            WHERE leader.department_id='${departmentId}'
    </select>

    <select id="selectPageByDepartmentIdsAndNotWorkGroupId" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
        LEFT JOIN dsm_department_user dept ON user.user_id=dept.user_id
        WHERE user.user_id NOT IN (SELECT user_id FROM dsm_work_group_user WHERE work_group_id=#{workGroupId})
        AND dept.department_id IN
        <foreach collection="list" item="id" index="index" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectPageByDepartmentIdsAndNotWorkGroupIdAndLike" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
        LEFT JOIN dsm_department_user dept ON user.user_id=dept.user_id
        WHERE user.user_id NOT IN (SELECT user_id FROM dsm_work_group_user WHERE work_group_id=#{workGroupId})
        AND (user.user_id LIKE '%${condition}%' OR user.login_name LIKE '%${condition}%' OR user.name LIKE '%${condition}%')
        AND dept.department_id IN
        <foreach collection="list" item="id" index="index" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectPageByNotWorkGroupId" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
            LEFT JOIN dsm_work_group_user wg ON  user.user_id=wg.user_id
            WHERE (wg.work_group_id != #{workGroupId} OR wg.work_group_id IS NULL)
    </select>

    <select id="selectPageByNotWorkGroupIdAndLike" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
            LEFT JOIN dsm_work_group_user wg ON  user.user_id=wg.user_id
            WHERE (wg.work_group_id != #{workGroupId} OR wg.work_group_id IS NULL)
                AND (user.user_id LIKE '%${condition}%' OR user.login_name LIKE '%${condition}%' OR user.name LIKE '%${condition}%')
    </select>

    <select id="selectPageByDepartmentIds" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
        LEFT JOIN dsm_department_user dept ON user.user_id=dept.user_id
        LEFT JOIN dsm_work_group_user wg ON  user.user_id=wg.user_id
        WHERE dept.department_id IN
        <foreach collection="list" item="id" index="index" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectPageByDepartmentIdsAndLike" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
        LEFT JOIN dsm_department_user dept ON user.user_id=dept.user_id
        LEFT JOIN dsm_work_group_user wg ON  user.user_id=wg.user_id
        WHERE (user.user_id LIKE '%${condition}%' OR user.login_name LIKE '%${condition}%' OR user.name LIKE '%${condition}%')
            AND dept.department_id IN
            <foreach collection="list" item="id" index="index" open="(" separator="," close=")">
                #{id}
            </foreach>
    </select>

    <select id="selectPageSimpleUserList" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
    </select>

    <select id="selectPageSimpleUserListByLike" parameterType="string" resultType="com.jkstack.dsm.user.controller.vo.UserSimpleVO">
        SELECT DISTINCT user.user_id, user.name,user.login_name FROM dsm_user user
        WHERE user.user_id LIKE '%${condition}%' OR user.login_name LIKE '%${condition}%' OR user.name LIKE '%${condition}%'
    </select>
</mapper>