server:
  port: 8081

nacos:
  server-addr: 192.168.3.23:8848
  namespace: jkstack-dsm

spring:
  profiles:
    active: ${spring.profiles.active}
  application:
    name: dsm-user
  cloud:
    gateway:
      routes:
        - id: dsm-user
          uri: lb://dsm-user
          predicates:
            - Path=/dsm-user/**
          filters:
            - StripPrefix=1
          order: 10

        - id: dsm-service-dir
          uri: lb://dsm-service-dir
          predicates:
            - Path=/dsm-service-dir/**
          filters:
            - StripPrefix=1
          order: 20
    nacos:
      discovery:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
      config:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
        file-extension: yml
        extension-configs[0]:
          group: DEFAULT_GROUP
          refresh: true
          data-id: zipkin.yml
        extension-configs[1]:
          group: DEFAULT_GROUP
          refresh: true
          data-id: redis.yml